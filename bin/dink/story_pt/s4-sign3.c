void main( void )
{
}

void talk( void )
{
if (&story > 10)
  {
  say_stop("`%Super-Fry Frango abrindo em breve.", &current_sprite);
  return;
  }

 freeze(1);
 say_stop("`%Escutem, escutem...", &current_sprite);
 say_stop("`%Por ordem dos nossos grandes patos, o imposto mensal será dobrado.", &current_sprite);
 say_stop("`%Isso também vale para doações de comida.", &current_sprite);
 say_stop("`%Vida longa aos patos.", &current_sprite;
 unfreeze(1);
}
